arch = [
    { package = "ly", hooks = { after_install = [ "sudo", "systemctl", "enable", "ly.service" ] } }, # Display manager

    # Apps
    "mpv", # Media player
    "viewnior",
    "legcord-bin", # Discord client
    "onlyoffice-bin", # Office suite
    "clipse", # Clipboard

    # File managers
    "yazi", # TUI
    "thunar", # GUI
    "catfish",
    "gvfs",
    "gvfs-mtp",
    "thunar-archive-plugin",
    "thunar-volman",

    # Thumbnails & previews
    "tumbler",
    "ffmpegthumbnailer",
    "libgsf",
    "poppler-glib",

    # Archiver
    "xarchiver",
    "7zip",
    "unrar",
    "unzip",
    "zip",

    # Gaming
    "steam",
    "gamescope",
    "protonup-qt",

    # GPU monitor and control
    { package = "lact", hooks = { after_install = [ "sudo", "systemctl", "enable", "--now", "lactd" ] } },

    # Shell
    # { package = "fish", hooks = { after_install = [ "chsh", "-s", "/usr/bin/fish" ] } }
]

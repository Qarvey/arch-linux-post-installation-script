enabled_backends = ["arch"]

hostname_groups_enabled = true

[hostname_groups]
cachy = [
  "base",
  "hardware",
  "filesystem",
  "network",
  "audio",
  "utils",
  "desktop",
  "hyprland",
  "fonts"
]

[arch]
package_manager = "yay"

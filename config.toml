enabled_backends = ["arch"]

hostname_groups_enabled = true

[hostname_groups]
cachy = [
  "minimal-cachyos-base",
  "hardware",
  "network",
  "audio",
  "utils",
  "desktop",
  "hyprland",
  "fonts"
]

[arch]
package_manager = "yay"

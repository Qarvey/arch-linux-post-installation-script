enabled_backends = ["arch"]

hostname_groups_enabled = true

[hostname_groups]
PLACEHOLDER = [
  "base",
  "hardware",
  "network",
  "audio",
  "utils",
  "fonts",
  "desktop",
  "hyprland"
]

[arch]
package_manager = "yay"
